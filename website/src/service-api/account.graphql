query Account($customerIdentifier: String!) {
  user {
    email
    firstName
    lastName
    phone
  }
  orders {
    data: getByCustomer(customerIdentifier: $customerIdentifier)
  }
  subscriptions {
    data: getByCustomer(customerIdentifier: $customerIdentifier)
  }
}
