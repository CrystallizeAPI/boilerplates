mutation TillitSearch($searchTerm: String!) {
  paymentProviders {
    tillit {
      search(searchTerm: $searchTerm) {
        success
        data {
          id
          name
          highlight
        }
      }
    }
  }
}
